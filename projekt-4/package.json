{
  "name": "express-stories",
  "version": "2.8.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "postinstall": "test -f .env || npm run generate_env",
    "generate_env": "./utils/generate_env.sh > .env",

    "predev": "test -f .env || npm run generate_env",
    "dev": "node --watch --env-file .env ./index.js",

    "precreate_admin": "test -f .env || npm run generate_env",
    "create_admin": "node --env-file .env utils/create_admin.js",

    "predb_fill": "test -f .env || npm run generate_env",
    "db_fill": "node --env-file .env utils/db_fill.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "argon2": "^0.44.0",
    "cookie-parser": "^1.4.7",
    "ejs": "^3.1.10",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
