<form action="/new_storyset" method="POST">
  <div>
    <label for="storyset_name">Nazwa kategorii</label>
    <input
      type="text"
      name="name"
      id="storyset_name"
      required
      minlength="3"
      maxlength="100"
      <% if (locals.name){ %>
      value="<%= name %>"
      <% }%>
    />
  </div>
  <div>
    <button>Dodaj</button>
  </div>
</form>
